<StackPanel x:Class="BIMPlugins.Common.WPF.SumView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:BIMPlugins.Common.WPF"
        d:DataContext="{d:DesignInstance local:SumViewModel}"
        mc:Ignorable="d" 
        Background="#FFF3D7C0"
        Orientation="Horizontal"
        Height="26"
        d:DesignWidth="860" >
    <StackPanel.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:CustomColorTheme BaseTheme="Light" PrimaryColor="#f88518" SecondaryColor="#FF0052A5" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </StackPanel.Resources>

    <TextBlock Text="SE Plugins" Margin="10 0 0 0" VerticalAlignment="Center" />
    <Border Width="4" BorderThickness="1 0" Background="Gray" Margin="10 0" />

    <TextBlock Text="Выбрано элементов:" VerticalAlignment="Center"/>
    <TextBlock Text="{Binding Count}" VerticalAlignment="Center" Margin="5 0"/>
    <Button Padding="10 2" VerticalAlignment="Center" Style="{StaticResource MaterialDesignPaperButton}" Height="22"
        Command="{Binding SelectElemsCommand}" Margin="10 0 0 0">
        <TextBlock Text="Выбрать" FontSize="12" FontWeight="DemiBold"/>
    </Button>
    <Border Width="4" BorderThickness="1 0" Background="Gray" Margin="10 0" />

    <TextBlock Text="Выберите параметр:" VerticalAlignment="Center"/>
    <ComboBox ItemsSource="{Binding Parameters, Mode=TwoWay}" SelectedItem="{Binding SelectedParameter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
              Width="160" Margin="5 0 0 0" VerticalAlignment="Center" Padding="2"
              DisplayMemberPath="Definition.Name" />
    <Border Width="4" BorderThickness="1 0" Background="Gray" Margin="10 0" />

    <TextBlock Text="Сумма:" VerticalAlignment="Center"/>
    <TextBox Text="{Binding Sum}" Margin="5 0 0 0" IsReadOnly="True" BorderThickness="0" Background="Transparent" Padding="5 0" 
                 VerticalAlignment="Center"  />
    <Border Width="4" BorderThickness="1 0" Background="Gray" Margin="10 0" />

    <Button Padding="10 2" VerticalAlignment="Center" Style="{StaticResource MaterialDesignPaperButton}" Height="22"
        Command="{Binding SumUpCommand}">
        <TextBlock Text="Готово" FontSize="12" FontWeight="DemiBold"/>
    </Button>
    <Button Padding="10 2" Margin="10 0 0 0" VerticalAlignment="Center" Style="{StaticResource MaterialDesignPaperButton}" Height="22"
        Command="{Binding CloseCommand}">
        <TextBlock Text="Закрыть" FontSize="12" FontWeight="DemiBold"/>
    </Button>
</StackPanel>

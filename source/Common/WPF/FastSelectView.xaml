<StackPanel x:Class="BIMPlugins.Common.WPF.FastSelectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:BIMPlugins.Common.WPF"
             d:DataContext="{d:DesignInstance local:FastSelectViewModel}"
             mc:Ignorable="d" 
             Background="#FFF3D7C0"
             Orientation="Horizontal"
             Height="26"
             d:DesignWidth="800" >
    <StackPanel.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:CustomColorTheme BaseTheme="Light" PrimaryColor="#f88518" SecondaryColor="#FF0052A5" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </StackPanel.Resources>

    <TextBlock Text="BIMPlugins" Margin="10 0 0 0" VerticalAlignment="Center"  />
    <Border Width="4" BorderThickness="1 0" Background="Gray" Margin="10 0" />
    
    <RadioButton Content="Текущий вид" VerticalAlignment="Center" GroupName="Options" Padding="2 0"/>
    <RadioButton Content="Вся модель" VerticalAlignment="Center" GroupName="Options" Margin="10 0 0 0" IsChecked="{Binding WholeModel}" Padding="2 0"/>
    <Border Width="4" BorderThickness="1 0" Background="Gray" Margin="10 0" />

    <Button Padding="10 2" Margin="10 0 0 0" VerticalAlignment="Center" Style="{StaticResource MaterialDesignPaperButton}" Height="22"
        Command="{Binding CategoryCommand}">
        <TextBlock Text="По категории" FontSize="12" FontWeight="DemiBold"/>
    </Button>
    <Button Padding="10 2" Margin="10 0 0 0" VerticalAlignment="Center" Style="{StaticResource MaterialDesignPaperButton}" Height="22"
            Command="{Binding FamilyCommand}">
        <TextBlock Text="По семейству" FontSize="12" FontWeight="DemiBold"/>
    </Button>
    <Button Padding="10 2" Margin="10 0 0 0" VerticalAlignment="Center" Style="{StaticResource MaterialDesignPaperButton}" Height="22"
            Command="{Binding CloseCommand}">
        <TextBlock Text="Отмена" FontSize="12" FontWeight="DemiBold"/>
    </Button>
</StackPanel>

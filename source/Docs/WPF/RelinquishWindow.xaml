<Window x:Class="BIMPlugins.Docs.WPF.RelinquishWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:BIMPlugins.Docs.WPF"
        d:DataContext="{d:DesignInstance local:RelinquishViewModel}"
        mc:Ignorable="d"
        Title="Освободить элементы" Height="300" Width="500" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:CustomColorTheme BaseTheme="Light" PrimaryColor="#f88518" SecondaryColor="#FF0052A5" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="AUTO" />
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Пользователь" FontWeight="SemiBold" Margin="5 0" VerticalAlignment="Center"/>
                <TextBox Text="{Binding Username}" Width="150" Height="22" materialDesign:HintAssist.Hint="Укажите имя" materialDesign:HintAssist.IsFloating="False"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}" materialDesign:TextFieldAssist.HasClearButton="True" Padding="3 0" Margin="5 0" />
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Width="25" Height="25" Padding="0" Margin="0" Command="{Binding ChooseFromRSNCommand}"
                        Style="{StaticResource MaterialDesignFlatButton}" ToolTip="Revit Server">
                    <Image Source="/BIMPlugins.Windows;component/Resources/server.png" Margin="2" />
                </Button>

                <Button Width="25" Height="25" Padding="0" Margin="5 0" Command="{Binding ChooseFromExplorerCommand}"
                        Style="{StaticResource MaterialDesignFlatButton}" ToolTip="Проводник">
                    <materialDesign:PackIcon Kind="FolderOutline" Width="25" Height="25" Foreground="#CC000000" />
                </Button>
            </StackPanel>
        </Grid>

        <GroupBox Padding="0" Margin="5">
            <GroupBox.Header>
                <TextBlock Text="Проект(ы)" FontWeight="SemiBold" />
            </GroupBox.Header>

            <ListBox ItemsSource="{Binding Files}" Padding="0" materialDesign:ListBoxItemAssist.ShowSelection="False" MinHeight="140" MaxHeight="180">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem" BasedOn="{StaticResource MaterialDesignListBoxItem}">
                        <Setter Property="Padding" Value="5 2" />
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding FilePath}" VerticalAlignment="Center" />
                            <Button Width="18" Height="18" Padding="0" Margin="5 0"
                                            Command="{Binding DataContext.RemoveProjectCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}"
                                            Style="{StaticResource MaterialDesignFlatButton}" ToolTip="Удалить">
                                <materialDesign:PackIcon Kind="WindowClose" Width="18" Height="18" />
                            </Button>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>

        <Button Content="Выполнить" Padding="0" Style="{StaticResource MaterialDesignPaperButton}" Margin="5 10 5 5"
                Command="{Binding RunCommand}" Height="25"/>
    </StackPanel>
</Window>
